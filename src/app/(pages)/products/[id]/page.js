import React from "react";
import ProductDetails from "../ProductDetails/ProductDetails";

const page = ({ params }) => {
  const id = params.id;

  console.log(id);

  return (
    <>
      {/* <ProductDetails /> */}
    </>
  );
};

export default page;
